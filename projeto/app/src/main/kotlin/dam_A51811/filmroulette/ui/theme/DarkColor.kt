package dam_A51811.filmroulette.ui.theme

import androidx.compose.ui.graphics.Color


/** Primary color of the theme. */
val Primary            = Color(0xFFFFB3B2)
/** Color used for text and icons displayed on top of the primary color. */
val OnPrimary          = Color(0xFF680012)
/** Color used for primary containers. */
val PrimaryContainer   = Color(0xFFFF525C)
/** Color used for text and icons displayed on top of primary containers. */
val OnPrimaryContainer = Color(0xFF5B000F)
/** Fixed primary color. */
val PrimaryFixed       = Color(0xFFFFDAD8)
/** Dimmed fixed primary color. */
val PrimaryFixedDim    = Color(0xFFFFB3B2)
/** Inverse primary color. */
val InversePrimary     = Color(0xFFBF002A)


/** Secondary color of the theme. */
val Secondary              = Color(0xFFFFDB9D)
/** Color used for text and icons displayed on top of the secondary color. */
val OnSecondary            = Color(0xFF412D00)
/** Color used for secondary containers. */
val SecondaryContainer     = Color(0xFFFEB700)
/** Color used for text and icons displayed on top of secondary containers. */
val OnSecondaryContainer   = Color(0xFF6B4B00)
/** Fixed secondary color. */
val SecondaryFixed         = Color(0xFFFFDEA8)
/** Dimmed fixed secondary color. */
val SecondaryFixedDim      = Color(0xFFFFBA20)


/** Tertiary color of the theme. */
val Tertiary             = Color(0xFF00DBE9)
/** Color used for text and icons displayed on top of the tertiary color. */
val OnTertiary           = Color(0xFF00363A)
/** Color used for tertiary containers. */
val TertiaryContainer    = Color(0xFF00A0AA)
/** Color used for text and icons displayed on top of tertiary containers. */
val OnTertiaryContainer  = Color(0xFF002F33)
/** Fixed tertiary color. */
val TertiaryFixed        = Color(0xFF7DF4FF)
/** Dimmed fixed tertiary color. */
val TertiaryFixedDim     = Color(0xFF00DBE9)


/** Color used to indicate errors. */
val Error          = Color(0xFFFFB4AB)
/** Color used for text and icons displayed on top of the error color. */
val OnError        = Color(0xFF690005)
/** Color used for error containers. */
val ErrorContainer = Color(0xFF93000A)
/** Color used for text and icons displayed on top of error containers. */
val OnErrorContainer = Color(0xFFFFDAD6)


/** Background color of the theme. */
val Background   = Color(0xFF131313)
/** Color used for text and icons displayed on the background color. */
val OnBackground = Color(0xFFE5E2E1)

/** Surface color of the theme. */
val Surface              = Color(0xFF131313)
/** Color used for text and icons displayed on surface colors. */
val OnSurface            = Color(0xFFE5E2E1)
/** Variant surface color. */
val SurfaceVariant       = Color(0xFF353534)
/** Color used for text and icons displayed on variant surface colors. */
val OnSurfaceVariant     = Color(0xFFE9BCBA)
/** Dimmed surface color. */
val SurfaceDim           = Color(0xFF131313)
/** Bright surface color. */
val SurfaceBright        = Color(0xFF393939)
/** Inverse surface color. */
val InverseSurface       = Color(0xFFE5E2E1)
/** Color used for text and icons displayed on inverse surface colors. */
val InverseOnSurface     = Color(0xFF313030)

/** Lowest elevation surface container color. */
val SurfaceContainerLowest  = Color(0xFF0E0E0E)
/** Low elevation surface container color. */
val SurfaceContainerLow     = Color(0xFF1C1B1B)
/** Base surface container color. */
val SurfaceContainer        = Color(0xFF201F1F)
/** High elevation surface container color. */
val SurfaceContainerHigh    = Color(0xFF2A2A2A)
/** Highest elevation surface container color. */
val SurfaceContainerHighest = Color(0xFF353534)


/** Color used for boundaries and outlines. */
val Outline        = Color(0xFFAF8786)
/** Variant color used for boundaries and outlines. */
val OutlineVariant = Color(0xFF5F3E3E)
