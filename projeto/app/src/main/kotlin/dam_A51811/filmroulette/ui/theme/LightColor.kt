package dam_A51811.filmroulette.ui.theme

import androidx.compose.ui.graphics.Color


/** Primary color for the light theme. */
val LightPrimary            = Color(0xFFBF002A) 
/** Color used for text and icons displayed on top of the primary color. */
val LightOnPrimary          = Color(0xFFFFFFFF)
/** Primary container color for the light theme. */
val LightPrimaryContainer   = Color(0xFFFFDAD8)
/** Color used for text and icons displayed on top of the primary container color. */
val LightOnPrimaryContainer = Color(0xFF41000B)

/** Secondary color for the light theme. */
val LightSecondary            = Color(0xFF7A5800)
/** Color used for text and icons displayed on top of the secondary color. */
val LightOnSecondary          = Color(0xFFFFFFFF)
/** Secondary container color for the light theme. */
val LightSecondaryContainer   = Color(0xFFFFDEA8)
/** Color used for text and icons displayed on top of the secondary container color. */
val LightOnSecondaryContainer = Color(0xFF271900)

/** Tertiary color for the light theme. */
val LightTertiary             = Color(0xFF006972)
/** Color used for text and icons displayed on top of the tertiary color. */
val LightOnTertiary           = Color(0xFFFFFFFF)
/** Tertiary container color for the light theme. */
val LightTertiaryContainer    = Color(0xFF7DF4FF)
/** Color used for text and icons displayed on top of the tertiary container color. */
val LightOnTertiaryContainer  = Color(0xFF002023)

/** Error color indicating validation errors or failures in the light theme. */
val LightError                = Color(0xFFBA1A1A)
/** Color used for text and icons displayed on top of the error color. */
val LightOnError              = Color(0xFFFFFFFF)
/** Error container color for the light theme. */
val LightErrorContainer       = Color(0xFFFFDAD6)
/** Color used for text and icons displayed on top of the error container color. */
val LightOnErrorContainer     = Color(0xFF410002)

/** Background color for the light theme. */
val LightBackground   = Color(0xFFF8F6F6)
/** Color used for text and icons displayed on the background color. */
val LightOnBackground = Color(0xFF1C1B1B)

/** Surface color for the light theme. */
val LightSurface              = Color(0xFFF8F6F6)
/** Color used for text and icons displayed on the surface color. */
val LightOnSurface            = Color(0xFF1C1B1B)
/** Surface variant color for the light theme. */
val LightSurfaceVariant       = Color(0xFFF5DDDC)
/** Color used for text and icons displayed on the surface variant color. */
val LightOnSurfaceVariant     = Color(0xFF534342)
/** Inverse surface color for the light theme. */
val LightInverseSurface       = Color(0xFF313030)
/** Color used for text and icons displayed on the inverse surface color. */
val LightInverseOnSurface     = Color(0xFFF4F0EF)

/** Outline color for boundaries and borders in the light theme. */
val LightOutline        = Color(0xFF857372)
/** Variant outline color for boundaries and borders in the light theme. */
val LightOutlineVariant = Color(0xFFD8C2C1)
